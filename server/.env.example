PORT=5000
MONGO_URL=mongodb://localhost:27017/alumni_db
JWT_SECRET=replace_with_a_secure_secret
FRONTEND_BASE_URL=http://localhost:5173

# SMTP settings (optional but required to send real emails)
# Example for Gmail: SMTP_HOST=smtp.gmail.com SMTP_PORT=587
# Make sure to use an app password if using Gmail
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=
